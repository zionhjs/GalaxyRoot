version: 0.2

phases:
  install:
    runtime-versions:
      java: openjdk8

  pre_build:
    commands:
      - echo jumpover.
  build:
    commands:
      - echo build started at `date`

  post_build:
    commands:
      - mvn clenan package -B
artifacts:
  files:
    - target/*.jar
    - appspec.yml
  discard-paths: yes
